import React from 'react';
import {Styles} from "../Style";

const Main = () => {
    const classes = Styles()
    return (
        <div className={classes.main}>
        </div>
    );
};

export default Main;